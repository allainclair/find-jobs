<!DOCTYPE html>
<html lang="en">

	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Jobs</title>
		<link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
	</head>

	<body class="bg-gray-100">
		<div class="container mx-auto p-4">
			<h1 class="text-3xl font-bold mb-4">Jobs from today ({{ jobs|length }})</h1>

			{% for job in jobs %}
			<div class="bg-white shadow-md rounded-lg mb-4 p-6">
				<h2 class="text-xl font-bold mb-2">{{ job.title }}</h2>
				<h3 class="text-gray-600 mb-2">{{ job.company }} - {{ job.location }} - {{ job.date }}</h3>
				<p class="text-gray-800 mb-4">{{ job.description }}</p>
				<p class="italic text-gray-600">Matching keywords ({{ job.matching_keywords|length }}): {{ ", ".join(job.matching_keywords) }}</p>
				<a href="{{ job.url }}" class="inline-block bg-purple-600 text-white font-semibold py-2 px-4 rounded hover:bg-purple-700">View Job</a>
			</div>
			{% endfor %}
		</div>
	</body>

</html>
